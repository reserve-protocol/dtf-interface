---
"@dtf-interface/react-sdk": patch
---

Add React Query wrappers for Index DTF optimistic governance, live proposal RPC state, and selector registry reads, with normalized unchecked address query keys.
