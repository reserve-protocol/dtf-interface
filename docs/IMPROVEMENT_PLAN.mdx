# DTF SDK Improvement Plan

Audited 2026-06-11; the P0–P3 roadmap from that audit was **executed the same day** on branch `chore/sdk-audit-improvements` (SDK) with the matching Register migration left uncommitted in `~/projects/register`. This document now records: what shipped, what the audit got wrong (verified during execution), and the next plan.

## Product framing (unchanged — drives all priorities)

- **Register is the main consumer and shapes the SDK**, but the SDK is becoming the main tool for dtf-ai, bots, and custom pages. Optimize for completeness and correctness.
- **Breaking changes are fine.** SDK and Register ship together in lockstep; no deprecation shims.
- **Register's partial adoption is intentional.** Unused hooks are future surface, not dead code.
- **react-sdk is the one-stop import** — `export * from "@reserve-protocol/sdk"` stays; everything (including ABIs) reachable through it.
- **dtf-catalog stays plain JSON.**
- **Money rule** (now in AGENTS.md): on-chain integer amounts are always `Amount { raw, formatted }` via `mapAmount`; plain `number` is reserved for display-class values (counts, timestamps, durations, percentages, rates, subgraph BigDecimal analytics).
- **Testing is rock solid as a pillar** — calldata encoding is the center of gravity.

---

## Executed (this session)

### P0 — Correctness & exposure

| Item                                 | Result                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `as never` cast in calldata encoding | `lib/contract-call.ts` now bridges viem's generic limitation with a precise `EncodeFunctionDataParameters<TAbi, TFunctionName>` cast + WHY comment. The caller-facing signature was always sound; viem validates function name and arg count at runtime (now proven by test).                                                                               |
| ABI export gap                       | All 20 distinct ABIs exported from the sdk root (timelock, optimistic timelock, selector registry, unstaking manager, both deployers, DAO fee registry, proposal ABI catalog + decoder set, v1/v2/v4 index ABIs, `folioArtifactAbi`). Pure rename aliases (`timelockProposalAbi`, `dtfIndexProposalAbiV1-V5`) deliberately not exported — one name per ABI. |
| staleTime defaults                   | `createDtfQueryOptions` applies 30s by default; price/auction/issuance builders pass `LIVE_STALE_TIME` (10s); identity/brand/version/list/discover/proposal-decode pass `STATIC_STALE_TIME` (5min). Constants exported. Overridable per query.                                                                                                              |

### P1 — Testing

New: exact-calldata fixture tests for `prepareContractCall`/`prepareErc20Approval` (including a runtime-validation proof that malformed args throw), an exact `castVote` calldata fixture, full `lib/utils` coverage, API transport error paths (SdkError meta, query param handling, network failure), `mergeProposalVotingSnapshot` tests, react-sdk staleTime class assertions, and the first react-sdk hook tests (proposal action hooks via jsdom + testing-library: undefined-param behavior, memo stability, provider errors). Workspace totals: **sdk 209 tests (+10 skipped live-smoke), react-sdk 50 tests**, all green.

### P2 — Breaking pass (SDK + Register lockstep)

| Item                           | Result                                                                                                                                                                                                                                                                                                                                                                                                   |
| ------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `get`/`getFull` duplication    | Removed `getFull`, `getFullIndexDtf`, `GetFullIndexDtfParams/Options`, `fullIndexDtfQueryOptions`, `useFullIndexDtf`. `get`/`getIndexDtf`/`useIndexDtf`/`indexDtfQueryOptions` are the one name. Register used none of the removed names.                                                                                                                                                                |
| Transaction mapper determinism | `getTransactions` no longer takes `dtfPriceUsd` or derives `amountUsd`.                                                                                                                                                                                                                                                                                                                                  |
| Vote-lock for unknown DTFs     | New `getVoteLockVaultState({ stToken, chainId, account })` reads drawer state straight from a staking vault (resolves underlying via `asset()` + erc20 metadata on-chain). Shares one core with `getVoteLockState`; prices now fetched in parallel with the multicall and **tolerated on failure** (price API outage no longer breaks the drawer). New `useIndexDtfVoteLockVaultState` hook + query key. |
| Proposal + snapshot merge      | `mergeIndexDtfProposalVotingSnapshot(proposal, snapshot)` absorbed from Register, including the optimistic threshold-keeping rule (documented with a WHY).                                                                                                                                                                                                                                               |
| Type helper dedup              | `IndexMethod`/`PortfolioMethod`/`MethodParams`/`MethodResult` now live once in `react-sdk/src/sdk-methods.ts` (were copy-pasted in 3 files).                                                                                                                                                                                                                                                             |
| Address utilities              | `dedupeAddresses` (case-sensitive, could double-count mixed-case guardians) deleted; `uniqueAddresses` is the one dedupe. Manual `.toLowerCase()` comparisons in touched governance files switched to `sameAddress` (two remain in `dtf/discovery.ts` and `propose/basket.ts` — sweep when next touching those files).                                                                                   |

### Register migration (uncommitted in `~/projects/register`, ships with the SDK bump)

- `use-fallback-vote-lock-state.ts` (~120 lines) **deleted**; `external-vote-lock-drawer.tsx` uses `useIndexDtfVoteLockVaultState`.
- `withVotingSnapshot()` deleted from `use-proposal-detail.ts`; uses `mergeIndexDtfProposalVotingSnapshot`.
- `use-proposal-type-eligibility.ts` uses the canonical `dtfQueryKeys.index.governance.selectorRegistryIsAllowed` key instead of a hand-rolled one (note: RQ v5 `useQueries` rejects the full branded options object, so the key + a plain `queryFn` is the dumb shape that works).
- **Verified**: Register typechecks clean against the locally built SDK (link test, then restored to `^0.1.5` — bump on release).

### Post-review fixes (Dark/Light Claude pass)

- Changeset added for the breaking release (minor bumps on sdk + react-sdk).
- Orphan `dtfQueryKeys.index.dtf` key deleted (zero consumers anywhere).
- `governance.mdx` now says the merge helper is a root export, not an `sdk.index` method.

### Docs

- `apps/docs` updated for every API change (get/getFull, vault state, merge helper, hooks tables); builds clean.
- **LLM guide** (`llm-guide.mdx`, consumed by reserve-ai) gained: caching defaults section, renamed/removed APIs section, vote-lock + snapshot helper guidance, and the full-ABI-surface rule ("never copy ABIs into a consumer project").

---

## Corrected findings (the audit was wrong; don't re-flag)

1. **"Governance domain lacks namespace/ref structure"** — false. `governance/namespace.ts` and `governance/ref.ts` exist and compose into the root factories. Topical read files (proposals/voting/delegates/…) beat a monolithic `reads.ts`; no restructure done or needed.
2. **"Money paths use Number()"** — mostly false. Everything weight/amount-like already maps through `mapAmount`; the flagged `Number()` calls are counts (`forDelegateVotes` is a delegate count, not weight), timestamps, durations, and display percentages. Revenue fields are subgraph BigDecimal analytics — display-class by design (NOTE comments added at the two ambiguous spots; rule added to AGENTS.md).
3. **"Vote-lock state lacks prices"** — stale. `getVoteLockState` already returned `underlyingPrice`. The real gap was the DTF-address requirement; fixed with the vault-keyed read.
4. **"Zero transport/API tests"** — overstated. `transports/subgraph` had 5 tests including error context; `client/api` had happy-path coverage. The real gaps (api error paths, contract-call, utils) are now filled.
5. **"as never produces silent malformed calldata"** — overstated. The public signature always type-checked callers, and viem throws at runtime on arg mismatches (now proven by a test). The fix still landed for honesty of the internal types.

## Intentional choices (don't re-litigate)

Security clean (no secrets, public Goldsky URLs, consistent `getAddress`). `export *` re-export is the design. dtf-catalog plain JSON, index-over-yield merge precedence now commented in code. `recent-proposals.ts` stays in Register (needs the tx receipt — inherently frontend). Proposal action hooks' repeated param extraction is deliberate explicit style. Register `mapProposalDetail` is Register's own legacy boundary — shrink only when its consumers change.

---

## Next plan

Ordered by value; nothing here blocks the release of what's above.

### Release (do first)

1. **Changesets + lockstep release.** One changeset covering the breaking changes (getFull removal, getTransactions param, vote-lock additions). Publish sdk + react-sdk, bump Register's `@reserve-protocol/react-sdk`, commit Register's migration files (4 files + 1 deletion), ship together. Register's vote-lock and governance flows should get a manual smoke pass (`/verify`) after the bump.

### Quality (next sessions)

2. **Hook test coverage expansion** (S–M): the jsdom harness now exists; add tests for `useIndexDtfProposalList` select behavior, `IndexDtfProvider` identity, and one query-flow hook with a mocked sdk. Target the hooks Register leans on hardest (`useIndexDtfIdentity`, `useIndexDtfProposal`).
3. **Propose-builder calldata fixtures** (M): `buildBasketProposal`/`buildSettingsProposal` produce governance calldata; they have behavior tests but not exact-encoding fixtures like `castVote` now has. Same pattern, highest-stakes remaining code.
4. **Discovery pagination ceiling** (S): `discovery.ts` still polls pages sequentially and defaults to a 1000-item subgraph cap. Fine today; document the cap in the JSDoc now, parallelize when DTF count makes it visible.

### API health (opportunistic)

5. **`useQueries` interop** (S): RQ v5's `UseQueryOptionsForUseQueries` rejects `DtfQueryOptionsResult` (branded `queryFn`/`placeholderData` variance). If more multi-query consumers appear, export a `forUseQueries()`-shaped builder or adopt RQ's `queryOptions()` branding in `createDtfQueryOptions`.
6. **Register `use-vote-lock-refresh.ts`** (S): still hand-manages invalidation keys; now that the fallback is an SDK hook with a canonical key, the external-refresh branch can likely invalidate `dtfQueryKeys.index.voteLockVaultState(...)` instead of threading refetch callbacks. Do it when next touching the drawer.
7. **`sdk.yield` boundary** (L, when Yield DTF work starts): placeholder methods throw `NOT_IMPLEMENTED`; the audit scope was Index-only. When yield work lands, repeat the calldata-fixture pattern from day one.

### Watchlist

- The `Number(bigint)` ratio in `votingWeight` rounds above 2^53 — harmless for a percentage, but if anyone ever sorts/compares by it for consensus-critical UI, switch to a Decimal ratio.
- `normalizeQueryKeyValue` doesn't normalize `number` vs `bigint` for the same value — irrelevant while params are typed, cheap to close if a cache-miss bug ever points there.

---

# Yield DTF SDK

Phase 1 shipped 2026-06-12 on `feat/yield-dtf-phase-1`. Protocol architecture reference: `docs/yield-dtf/PROTOCOL.md`. Behavioral source of truth: Register's `src/state/rtoken/*` and `src/views/yield-dtf/*`.

## Phase 1 — shipped

- ABIs (facade-read/act, r-token, main, st-rsr, st-rsr-votes, basket-handler), facade + RSR addresses for 1/8453, `YieldDtfChainId`.
- Subgraph codegen target against the deployed `dtf-yield` Goldsky subgraph + phase-1 queries.
- Core reads: `get` (subgraph identity + on-chain mandate/basket tokens), `list`, `getContracts` (Main components), `getState` (supply/throttles/pauses — protocol >= 3.0 only), `getPrice` (facade), `getBasket` (breakdown/backing/buffer), `getHolders`, `getTransactions`.
- Issuance: facade callStatic quotes (`getIssuanceQuote`/`getRedemptionQuote`/`getMaxIssuable`), `prepareIssue(+Plan)`, `prepareRedeem`, `prepareRedeemCustom`.
- Staking: `getStakingState` (incl. pendingUnstakings via the draftEra storage slot `0x109` — no public getter exists), `getStakeHistory`, `prepareStake(+Plan)`, `prepareUnstake`, `prepareWithdraw`, `prepareCancelUnstake`.
- `createYieldDtfNamespace` + `createYieldDtfRef`; `sdk.yield` placeholder replaced.
- react-sdk: `dtfQueryKeys.yield.*`, 13 query option builders + hooks with staleTime classes, 5 call hooks.
- Tests: exact-calldata fixtures for every prepare builder, frozen subgraph mapper fixtures, key/staleTime/call-hook tests, live smoke against mainnet eUSD (all 4 passing live).

## Phase 2 — governance

1. **Extract shared OZ governor call builders** from `index-dtf/governance/proposal-actions.ts` into `lib/` (vote/voteWithReason/queue/execute/cancel/propose + description hashing + legacy timelock salt). Both domains consume; index tests keep passing unchanged.
2. Subgraph reads: `getYieldDtfGovernance` (governance + governanceFrameworks + guardians under `governance(id: rtoken)`), `getYieldDtfProposals`/`getYieldDtfProposal` (Proposal entity with targets/values/calldatas/state/votes), `getYieldDtfDelegates`, voter state (TokenHolder + Delegate + on-chain `getVotes`).
3. **Anastasius handling**: governors named "Governance Anastasius" use timepoints — read `quorum(timepoint)`/`proposalThreshold()` on-chain to override stale subgraph values (Register `RTokenGovernanceUpdater` is the reference). Alexios (block-based) uses subgraph values directly.
4. Proposal actions: prepare vote/queue/execute/cancel against the per-DTF Governor + Timelock via the shared builders.
5. react-sdk hooks mirroring the index governance set.
6. Fixture tests with the OZ selectors; live smoke on a real proposal.

## Phase 3 — auctions & revenue

1. ABIs: broker, dutch-trade, gnosis-trade, backing-manager, revenue-trader, distributor, furnace (from Register `src/abis`).
2. Reads: `getYieldDtfRevenueOverview` (`FacadeAct.revenueOverview` per trader: surpluses/canStart/minTradeAmounts), `getYieldDtfSettleableAuctions` (`FacadeRead.auctionsSettleable`), `getYieldDtfNextRecollateralization` (`FacadeAct.nextRecollateralizationAuction`), `getYieldDtfTrades` (subgraph Trade history), dutch auction status + `bidAmount(timestamp)` quotes.
3. Builders: `prepareYieldDtfRunRevenueAuctions` (FacadeAct multicall: toSettle/toStart/kinds), `prepareYieldDtfRebalance` (BackingManager), `prepareYieldDtfBid` (DutchTrade bid + approval plan), `prepareYieldDtfClaimRewards` (FacadeAct).
4. Register reference: `src/views/yield-dtf/auctions/atoms.ts` + `useAuctions.ts` (availableAuctionRevenue/pendingToMelt/claimable computation).

## Phase 4 — APY, proposal building, Register migration

1. **APY (SDK-owned per product decision)**: absorb Register's DefiLlama pool-id→symbol map (`CollateralYieldUpdater.tsx`, per-chain) into the SDK; `getCollateralYields(chainId)` fetch helper + pure `computeYieldDtfApy({ basketShares, collateralYields, revenueSplit, supplyUsd, stakedUsd })` returning `{ basket, holders, stakers }` (stakers leveraged by supplyUsd/stakedUsd). Realized staking APY from `RTokenDailySnapshot.rsrExchangeRate` growth: new snapshot query + `getYieldDtfStakingApyHistory`.
2. **Proposal building**: parameter contract map (Register `parameterContractMapAtom`) → `prepareYieldDtfSet*` builders (tradingDelay, backingBuffer, maxTradeSlippage, minTrade, rewardRatio, unstakingDelay, withdrawalLeak, warmupPeriod, issuancePremium, auction lengths, throttles, freezes), role grant/revoke, basket + backup config changes, asset (un)registration, upgrade spells. Compose into governor `propose` calldata via the shared builders.
3. **Register migration begins**: replace Register's rToken atoms/updaters with SDK hooks view by view (overview → issuance → staking → governance → auctions), same lockstep release pattern as Index DTF.

## Post-review fixes (Dark/Light Claude pass)

- **Critical (caught by review, verified against live data)**: `Entry.amount` is raw BigInt, not BigDecimal — `getYieldDtfTransactions` now maps it to `Amount`; the live smoke asserts amount-vs-USD magnitude so a units regression fails loudly.
- `BURN` entries are furnace melts and map to `"burn"`, distinct from `"redeem"`; `MINT` maps to `"mint"`.
- Revenue split ports Register's sum-to-10000 rounding patch.
- `list` now merges dtf-catalog metadata (`status`, `logo`) per the original plan.
- Added `useYieldDtfCancelUnstakeCall`; yield full/list staleTime corrected to the default class (they carry supply/price data); `unstakingDelay` is a display-class number; over-fetched subgraph fields trimmed.
- Intentional divergences from Register documented: backing/staked percentages are unrounded (Register ceils for UI); `targetAmount` is the contract-true value (Register stores it x100 for UI) — Phase 4 proposal builders must not double-apply that convention.

## Known boundaries

- Protocol 2.x mains (legacy `paused()`) unsupported — all live listed Yield DTFs are >= 3.0.
- Arbitrum has deployed Yield DTFs but no configured subgraph in the SDK; add `42161` to `YieldDtfChainId` + subgraph URL when needed.
- `Collateral` subgraph entity has no decimals — `get` does an on-chain ERC20 metadata pass for basket tokens (one extra multicall).
