---
"@reserve-protocol/sdk": patch
"@reserve-protocol/react-sdk": patch
---

Support self-appreciating vote-lock vaults (vlRSR): `prepareVoteLockRedeem` builder for shares-denominated unlocks, `VoteLockState.shareBalance` + `VoteLockState.exchangeRate` (same single multicall), and `useIndexDtfVoteLockPreviewDeposit` / `useIndexDtfVoteLockPreviewRedeem` hooks for lock/unlock quotes.
