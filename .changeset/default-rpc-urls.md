---
"@reserve-protocol/sdk": patch
---

Refresh `DEFAULT_RPC_URLS` with stress-tested keyless endpoints: drop the tenderly gateways and the slower BSC dataseed mirrors, add drpc/sentio/blastapi/blockpi/bnbchain.
