{
  "name": "dtf-interface",
  "version": "0.0.0",
  "private": true,
  "description": "TypeScript interface SDK for DTF integrations.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/reserve-protocol/dtf-interface"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean",
    "dev": "turbo run dev",
    "docs:build": "pnpm --filter @reserve-protocol/docs build",
    "docs:dev": "pnpm --filter @reserve-protocol/docs dev",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check",
    "graphql:codegen": "pnpm --filter @reserve-protocol/sdk graphql:codegen",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix --fix-suggestions --fix-dangerously",
    "playground:index": "pnpm --filter @reserve-protocol/sdk playground:index",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "changeset publish",
    "release:ci": "pnpm typecheck && pnpm test && pnpm build && pnpm docs:build && pnpm --filter @reserve-protocol/dtf-catalog pack --dry-run && pnpm --filter @reserve-protocol/sdk pack --dry-run && pnpm --filter @reserve-protocol/react-sdk pack --dry-run",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@changesets/config": "^3.1.4",
    "oxfmt": "^0.46.0",
    "oxlint": "^1.63.0",
    "turbo": "^2.9.9"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.33.0"
}
